<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:title="Меню колоды" />
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- твой LinearLayout внутри -->


    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- Заголовок / имя колоды -->
            <TextView
                android:id="@+id/tvDeckTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="1. Слова 1급"
                android:textAppearance="@style/TextAppearance.Material3.TitleLarge" />

            <!-- Краткое описание колоды -->
            <TextView
                android:id="@+id/tvDeckDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="Базовая лексика уровня 1급: самые частотные слова для ежедневной практики."
                android:textAppearance="@style/TextAppearance.Material3.BodyLarge" />

            <!-- Кнопка для перехода на отдельный экран со списком слов -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnWordList"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:text="Список слов" />

            <!-- Кнопка начать изучение (как на скрине) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnStudy"
                style="@style/Widget.Material3.Button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="Изучать" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>


